name: CI
permissions:
  contents: read
concurrency:
  group: ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true
on:
  pull_request:
  workflow_dispatch:
    inputs:
      packages:
        description: "Space-separated check attrs to build across all supported Linux runners"
        required: false
        default: ""
      buildAllPackages:
        description: "Build all Linux check attrs on every supported Linux runner"
        required: false
        type: boolean
        default: false
jobs:
  discoverPackages:
    if: github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && (inputs.buildAllPackages || inputs.packages != ''))
    runs-on: ubuntu-24.04
    outputs:
      hasPackages: ${{ steps.discover.outputs.hasPackages }}
      matrix: ${{ steps.discover.outputs.matrix }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
      - uses: cachix/install-nix-action@v31
      - id: discover
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          BUILD_ALL_PACKAGES: ${{ github.event_name == 'workflow_dispatch' && inputs.buildAllPackages }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          PACKAGES: ${{ inputs.packages }}
        run: nix develop -c deno run --allow-env --allow-read --allow-run --allow-write="$GITHUB_OUTPUT" .github/ci/discoverCiPackageBuilds.ts
  check:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@v31
      - run: nix fmt -- --ci
      - run: nix develop -c actionlint
      - run: nix develop -c deno task check
      - run: nix develop -c deno task lint
      - run: nix develop -c deno task test
      - name: Realize bun2nix cache entry creator
        run: |
          system="$(nix eval --impure --raw --expr builtins.currentSystem)"
          nix build --no-link --inputs-from . "bun2nix#packages.$system.cacheEntryCreator" --print-build-logs
      - run: nix flake check --no-build
  buildPackages:
    needs: discoverPackages
    if: needs.discoverPackages.outputs.hasPackages == 'true'
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discoverPackages.outputs.matrix) }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: cachix/install-nix-action@v31
      - run: nix build ".#checks.${{ matrix.system }}.${{ matrix.package }}" --print-build-logs
  packageBuildGate:
    needs:
      - discoverPackages
      - buildPackages
    if: always() && (github.event_name == 'pull_request' || (github.event_name == 'workflow_dispatch' && (inputs.buildAllPackages || inputs.packages != '')))
    runs-on: ubuntu-24.04
    steps:
      - run: |
          if [ "${{ needs.discoverPackages.result }}" != success ]; then
            echo "Package discovery failed"
            exit 1
          fi

          if [ "${{ needs.discoverPackages.outputs.hasPackages }}" = true ]; then
            if [ "${{ needs.buildPackages.result }}" != success ]; then
              echo "Package builds failed"
              exit 1
            fi
          else
            echo "No package builds required"
          fi
