diff --git a/src/specify_cli/extensions/__init__.py b/src/specify_cli/extensions/__init__.py
index 01067a7..2f8f637 100644
--- a/src/specify_cli/extensions/__init__.py
+++ b/src/specify_cli/extensions/__init__.py
@@ -33,6 +33,15 @@ from ..catalogs import CatalogEntry as BaseCatalogEntry
 from ..catalogs import CatalogStackBase
 from ..shared_infra import verify_archive_sha256

+def _normalize_copied_tree_permissions(path: Path) -> None:
+    for item in [path, *path.rglob("*")]:
+        mode = item.stat().st_mode
+        if item.is_dir():
+            item.chmod(mode | 0o700)
+        else:
+            item.chmod(mode | 0o600)
+
+
 _FALLBACK_CORE_COMMAND_NAMES = frozenset(
     {
         "analyze",
@@ -1352,6 +1361,7 @@ class ExtensionManager:

         ignore_fn = self._load_extensionignore(source_dir)
         shutil.copytree(source_dir, dest_dir, ignore=ignore_fn)
+        _normalize_copied_tree_permissions(dest_dir)

         # Register commands with AI agents
         registered_commands = {}
diff --git a/src/specify_cli/presets/__init__.py b/src/specify_cli/presets/__init__.py
index a6b2ded..c1dfab6 100644
--- a/src/specify_cli/presets/__init__.py
+++ b/src/specify_cli/presets/__init__.py
@@ -33,6 +33,15 @@ from ..integrations.base import IntegrationBase
 from .._utils import dump_frontmatter
 from ..shared_infra import verify_archive_sha256

+def _normalize_copied_tree_permissions(path: Path) -> None:
+    for item in [path, *path.rglob("*")]:
+        mode = item.stat().st_mode
+        if item.is_dir():
+            item.chmod(mode | 0o700)
+        else:
+            item.chmod(mode | 0o600)
+
+
 def _substitute_core_template(
     body: str,
     cmd_name: str,
@@ -1541,6 +1550,7 @@ class PresetManager:
             shutil.rmtree(dest_dir)

         shutil.copytree(source_dir, dest_dir)
+        _normalize_copied_tree_permissions(dest_dir)
 
         # Pre-register the preset so that composition resolution can see it
         # in the priority stack when resolving composed command content.
diff --git a/src/specify_cli/commands/init.py b/src/specify_cli/commands/init.py
index 60513b7..4a40539 100644
--- a/src/specify_cli/commands/init.py
+++ b/src/specify_cli/commands/init.py
@@ -465,13 +465,13 @@ def register(app: typer.Typer) -> None:
                                 project_path / ".specify" / "workflows" / "speckit"
                             )
                             dest_wf.mkdir(parents=True, exist_ok=True)
+                            workflow_file = dest_wf / "workflow.yml"
                             _shutil.copy2(
                                 bundled_wf / "workflow.yml",
-                                dest_wf / "workflow.yml",
+                                workflow_file,
                             )
-                            definition = WorkflowDefinition.from_yaml(
-                                dest_wf / "workflow.yml"
-                            )
+                            workflow_file.chmod(workflow_file.stat().st_mode | 0o600)
+                            definition = WorkflowDefinition.from_yaml(workflow_file)
                             wf_registry.add(
                                 "speckit",
                                 {
diff --git a/src/specify_cli/__init__.py b/src/specify_cli/__init__.py
index 4c62178..c5d641e 100644
--- a/src/specify_cli/__init__.py
+++ b/src/specify_cli/__init__.py
@@ -1023,7 +1023,9 @@ def workflow_add(

         dest_dir = workflows_dir / definition.id
         dest_dir.mkdir(parents=True, exist_ok=True)
         import shutil
-        shutil.copy2(yaml_path, dest_dir / "workflow.yml")
+        workflow_file = dest_dir / "workflow.yml"
+        shutil.copy2(yaml_path, workflow_file)
+        workflow_file.chmod(workflow_file.stat().st_mode | 0o600)
         registry.add(definition.id, {
             "name": definition.name,
             "version": definition.version,
@@ -1192,6 +1194,7 @@ def workflow_add(
                 )
                 raise typer.Exit(1)
             workflow_file.write_bytes(response.read())
+            workflow_file.chmod(workflow_file.stat().st_mode | 0o600)
     except Exception as exc:
         if workflow_dir.exists():
             import shutil
