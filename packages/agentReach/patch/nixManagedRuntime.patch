diff --git a/agent_reach/cli.py b/agent_reach/cli.py
--- a/agent_reach/cli.py
+++ b/agent_reach/cli.py
@@ -174,6 +174,6 @@ def _cmd_install(args):
     from agent_reach.doctor import check_all, format_report
 
-    safe_mode = args.safe
+    safe_mode = args.safe or os.environ.get("AGENT_REACH_NIX_MANAGED") == "1"
     dry_run = args.dry_run
 
     config = Config()
@@ -184,7 +184,8 @@ def _cmd_install(args):
 
     # Ensure tools directory exists (for upstream tool repos)
     tools_dir = os.path.expanduser("~/.agent-reach/tools")
-    os.makedirs(tools_dir, exist_ok=True)
+    if not dry_run and not safe_mode:
+        os.makedirs(tools_dir, exist_ok=True)
 
     if dry_run:
         print("DRY RUN — showing what would be done (no changes)")
@@ -320,6 +321,8 @@ def _cmd_install(args):
 
         # ── Install agent skill ──
-        _install_skill()
-
-        print(f"✅ Installation complete! {ok}/{total} channels active.")
+        if safe_mode:
+            print(f"Safe mode check complete. No changes were made. {ok}/{total} channels active.")
+        else:
+            _install_skill()
+            print(f"✅ Installation complete! {ok}/{total} channels active.")
 
         if not requested_channels:
diff --git a/agent_reach/config.py b/agent_reach/config.py
--- a/agent_reach/config.py
+++ b/agent_reach/config.py
@@ -36,7 +36,6 @@ class Config:
         self.config_path = Path(config_path) if config_path else self.CONFIG_FILE
         self.config_dir = self.config_path.parent
         self.data: dict = {}
-        self._ensure_dir()
         self.load()
 
     def _ensure_dir(self):
diff --git a/pyproject.toml b/pyproject.toml
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -34,6 +34,5 @@ dependencies = [
     "pyyaml>=6.0",
     "rich>=13.0",
-    "yt-dlp>=2024.0",
 ]
 
 [project.optional-dependencies]
diff --git a/agent_reach/channels/youtube.py b/agent_reach/channels/youtube.py
--- a/agent_reach/channels/youtube.py
+++ b/agent_reach/channels/youtube.py
@@ -2,6 +2,7 @@
 """YouTube — check if yt-dlp is available with JS runtime."""
 
 import shutil
+import sys
 
 from agent_reach.probe import probe_command
 from agent_reach.utils.paths import get_ytdlp_config_path, render_ytdlp_fix_command
@@ -30,7 +31,12 @@ class YouTubeChannel(Channel):
 
     def check(self, config=None):
         # 真跑 yt-dlp --version 探活，区分未装 / venv 断链 / 跑不动
-        probe = probe_command("yt-dlp", ["--version"], timeout=10, package="yt-dlp")
+        probe = probe_command(
+            "yt-dlp",
+            ["--version"],
+            timeout=30 if sys.platform == "darwin" else 10,
+            package="yt-dlp",
+        )
         if probe.status == "missing":
             self.active_backend = None
             return "off", "yt-dlp 未安装。安装：pip install yt-dlp"
